<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Aura" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
        <item name="colorSecondary">@color/light_gery</item>
        <item name="colorTertiary">@color/white</item>
        <item name="colorAccent">@color/main_color</item>
        <item name="color">@color/back_color</item>
    </style>

    <style name="Theme.Aura" parent="Base.Theme.Aura" />
</resources>